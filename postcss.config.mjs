// const config = {
//   plugins: ["@tailwindcss/postcss"],
// };

// export default config;

// postcss.config.js (or postcss.config.ts if you're using TS)
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
