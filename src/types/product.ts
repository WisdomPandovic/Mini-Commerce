export type Product = {
    id: string
    slug: string
    name: string
    header: string
    description: string
    image: string
    price: number
    category: string
    rating: number
    sizes?: string[]
    colors?: string[]
  }
  